---
import Base from '../layouts/Base.astro';
import { etsyLink } from '../data/site';

const tools = [
	{ name: "Brain Dump", desc: "Clear the noise.", href: "/tools/brain-dump", tag: "ADHD", color: "purple" },
	{ name: "Weekly Reset", desc: "Turn a scattered week into a clear plan.", href: "/tools/weekly-reset", tag: "Productivity", color: "green" },
	{ name: "Morning Routine", desc: "Build a routine you'll actually use.", href: "/tools/morning-routine", tag: "ADHD", color: "amber" },
];

const pillars = [
	{ num: "01", title: "ADHD", desc: "Planning, routines, and systems built around how an ADHD brain actually works.", href: "/adhd", color: "green" },
	{ num: "02", title: "Students", desc: "Homework, study planning, and getting assignments done without the spiral.", href: "/students", color: "amber" },
	{ num: "03", title: "Productivity", desc: "Weekly planning, prioritizing, and routines that don't require willpower.", href: "/productivity", color: "purple" },
];

const siteUrl = new URL(Astro.url.pathname, Astro.site);
const orgSchema = {
	"@context": "https://schema.org",
	"@type": "Organization",
	"name": "SteadyFocusCo",
	"url": new URL("/", Astro.site).toString(),
	"logo": new URL("/logo.png", Astro.site).toString(),
	"description": "ADHD-friendly planners, printables, checklists and free productivity tools designed to reduce overwhelm, improve focus and make planning easier.",
};
const webSiteSchema = {
	"@context": "https://schema.org",
	"@type": "WebSite",
	"name": "SteadyFocusCo",
	"url": new URL("/", Astro.site).toString(),
	"description": "ADHD-friendly planners, printables, checklists and free productivity tools designed to reduce overwhelm, improve focus and make planning easier.",
};
---
<Base
	title="ADHD Planners & Productivity Tools for Overwhelm | SteadyFocusCo"
	description="ADHD-friendly planners, printables, checklists and free productivity tools designed to reduce overwhelm, improve focus and make planning easier."
>
	<script type="application/ld+json" set:html={JSON.stringify(orgSchema)} />
	<script type="application/ld+json" set:html={JSON.stringify(webSiteSchema)} />
	<!-- ===== HERO ===== -->
	<section class="hero" id="hero">
		<div class="container">
			<div class="hero-grid">
				<div class="hero-copy">
					<p class="eyebrow">ADHD-friendly planning</p>
					<h1>ADHD planners, printables & <span class="text-accent">productivity tools</span> that reduce overwhelm.</h1>
					<p class="hero-lede">Free ADHD productivity tools, executive-function supports, and printable planners that turn scattered thoughts into clear next steps — built for daily routines and focus.</p>
					<div class="hero-actions">
						<a href="/shop" class="btn btn-primary">Explore Planners</a>
						<a href="/tools" class="btn btn-secondary">Free Tools</a>
					</div>
					<p class="hero-trust">Designed with care <span>·</span> Made to help <span>·</span> Here for you.</p>
				</div>
				<div class="hero-visual">
					<div class="hero-image-wrap">
						<img src="/products/hero-planner.png" alt="SteadyFocusCo weekly planner flat-lay with planner pages, daily focus sheet, and pen" loading="eager" fetchpriority="high" />
						<div class="hero-float-card">
							<p class="hero-float-title">Made for how your brain works.</p>
							<ul class="hero-float-list">
								<li>Reduce overwhelm</li>
								<li>Build routines</li>
								<li>Stay consistent</li>
								<li>Feel more in control</li>
							</ul>
						</div>
					</div>
				</div>
			</div>
		</div>
	</section>

	<!-- ===== FEATURE STRIP ===== -->
	<section class="feature-strip">
		<div class="container">
			<div class="feature-grid">
				<div class="feature">
					<div class="feature-icon" aria-hidden="true">
						<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2a7 7 0 0 1 7 7c0 2.5-1.5 4.5-3 6s-3 3.5-3 5.5"/><path d="M12 2a7 7 0 0 0-7 7c0 2.5 1.5 4.5 3 6s3 3.5 3 5.5"/><circle cx="12" cy="9" r="2.5"/></svg>
					</div>
					<div>
						<p class="feature-title">ADHD-Friendly</p>
						<p class="feature-desc">Designed for real life and real brains.</p>
					</div>
				</div>
				<div class="feature">
					<div class="feature-icon" aria-hidden="true">
						<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="6" y="3" width="12" height="18" rx="2"/><path d="M9 7h6"/><path d="M9 11h6"/><path d="M9 15h4"/></svg>
					</div>
					<div>
						<p class="feature-title">Printable & Reusable</p>
						<p class="feature-desc">Use it your way, anytime.</p>
					</div>
				</div>
				<div class="feature">
					<div class="feature-icon" aria-hidden="true">
						<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3v12"/><path d="M7 10l5-7 5 7"/><path d="M5 17h14"/><path d="M7 21h10"/></svg>
					</div>
					<div>
						<p class="feature-title">Instant Access</p>
						<p class="feature-desc">Download and start today.</p>
					</div>
				</div>
				<div class="feature">
					<div class="feature-icon" aria-hidden="true">
						<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/></svg>
					</div>
					<div>
						<p class="feature-title">Made with Care</p>
						<p class="feature-desc">Thoughtfully designed to support you.</p>
					</div>
				</div>
			</div>
		</div>
	</section>

	<!-- ===== SCROLL STORY: CHAOS ===== -->
	<section class="story" id="story">
		<div class="container">
			<div class="story-inner">
				<div class="story-copy">
					<p class="eyebrow">The problem</p>
					<h2>Your brain isn't a checklist.</h2>
					<p class="section-lede">Scattered thoughts pile up until everything feels urgent. The fix isn't more willpower — it's getting it out of your head and into a system.</p>
				</div>
				<div class="story-visual">
					<img src="/products/brain-dump-card.png" alt="ADHD brain dump printable showing scattered thoughts organized into a clear system" loading="lazy" />
				</div>
			</div>
		</div>
	</section>

	<!-- ===== SCROLL STORY: SORTING ===== -->
	<section class="sorting">
		<div class="container">
			<svg class="leaf-accent leaf-sorting" viewBox="0 0 140 100" aria-hidden="true"><g fill="#97C459"><ellipse cx="20" cy="80" rx="8" ry="20" transform="rotate(-20 20 80)"/><ellipse cx="40" cy="90" rx="7" ry="18" transform="rotate(-5 40 90)"/><ellipse cx="5" cy="60" rx="6" ry="16" transform="rotate(-45 5 60)"/></g></svg>
			<div class="sorting-inner">
				<div class="sorting-copy">
					<p class="eyebrow">The method</p>
					<h2>Sort it once. See the next step.</h2>
					<p class="section-lede">Everything lands in one of three places — do it now, it has a deadline, or it can wait. That's the whole system.</p>
				</div>
				<div class="sorting-visual">
					<div class="sorting-flow">
						<div class="flow-in"><span>Scattered</span></div>
						<div class="flow-arrow">→</div>
						<div class="flow-out">
							<div><strong>Now</strong></div>
							<div><strong>Deadline</strong></div>
							<div><strong>Later</strong></div>
						</div>
					</div>
				</div>
			</div>
		</div>
	</section>

	<!-- ===== SCROLL STORY: CLARITY ===== -->
	<section class="clarity">
		<div class="container">
			<div class="clarity-inner">
				<p class="eyebrow">The result</p>
				<h2>Clear the noise. Find the next step.</h2>
				<p class="section-lede">A calm, organized plan you can actually start — not another list you'll ignore.</p>
			</div>
		</div>
	</section>

	<!-- ===== TOOLS ===== -->
	<section class="tools" id="tools">
		<div class="container">
			<div class="section-head">
				<p class="eyebrow">Free tools</p>
				<h2>Tools for when your brain feels full</h2>
			</div>
			<div class="tools-grid">
				{tools.map((t) => (
					<a class={`tool-card c-${t.color}`} href={t.href}>
						<div class="tool-surface">
							<span class="tool-tag">{t.tag}</span>
							<h3>{t.name}</h3>
							<p>{t.desc}</p>
							<span class="tool-link">Try it free <span class="arrow">→</span></span>
						</div>
					</a>
				))}
			</div>
		</div>
	</section>

	<!-- ===== PATHS ===== -->
	<section class="paths">
		<div class="container">
			<div class="section-head">
				<p class="eyebrow">Choose your path</p>
				<h2>Start with a problem</h2>
			</div>
			<div class="paths-grid">
				{pillars.map((p) => (
					<a class={`path-card c-${p.color}`} href={p.href}>
						<span class="path-num">{p.num}</span>
						<h3>{p.title}</h3>
						<p>{p.desc}</p>
						<span class="path-link">Explore <span class="arrow">→</span></span>
					</a>
				))}
			</div>
		</div>
	</section>

	<!-- ===== EXPLORE MORE ===== -->
	<section class="explore">
		<div class="container">
			<div class="section-head">
				<p class="eyebrow">Keep exploring</p>
				<h2>More ways to plan with less overwhelm</h2>
			</div>
			<div class="explore-grid">
				<a class="explore-card" href="/guides">
					<h3>Guides</h3>
					<p>Practical ADHD planning, executive-function, and study guides written in plain language.</p>
					<span class="explore-link">Read the guides <span class="arrow">→</span></span>
				</a>
				<a class="explore-card" href="/printables">
					<h3>Free printables</h3>
					<p>Reusable ADHD printables and checklists you can download and print at home.</p>
					<span class="explore-link">Browse printables <span class="arrow">→</span></span>
				</a>
				<a class="explore-card" href="/shop">
					<h3>Shop</h3>
					<p>ADHD planners and printable systems for students, adults, and daily routines.</p>
					<span class="explore-link">Visit the shop <span class="arrow">→</span></span>
				</a>
			</div>
		</div>
	</section>

	<!-- ===== ETSY / PRINTABLES ===== -->
	<section class="printables">
		<div class="container">
			<div class="printables-inner">
				<svg class="leaf-accent leaf-printables" viewBox="0 0 100 100" aria-hidden="true"><g fill="#fff"><ellipse cx="50" cy="30" rx="7" ry="17" transform="rotate(20 50 30)"/><ellipse cx="65" cy="45" rx="6" ry="14" transform="rotate(55 65 45)"/><ellipse cx="35" cy="45" rx="6" ry="14" transform="rotate(-30 35 45)"/></g></svg>
				<div class="printables-copy">
					<p class="eyebrow">Printables</p>
					<h2>Take the system off-screen.</h2>
					<p class="section-lede">Prefer paper? The same planning systems are available as printable planners — instant download, designed to reuse.</p>
					<a class="btn btn-on-color" href={etsyLink({ medium: "homepage", campaign: "homepage" })} target="_blank" rel="sponsored noopener" data-analytics-event="etsy_click">Browse printable planners →</a>
				</div>
				<div class="printables-visual">
					<div class="planner">
						<div class="planner-page page-back"></div>
						<div class="planner-page page-mid"></div>
						<div class="planner-page page-front">
							<span class="planner-line"></span>
							<span class="planner-line"></span>
							<span class="planner-line short"></span>
						</div>
						<div class="planner-spine"></div>
					</div>
				</div>
			</div>
		</div>
	</section>
</Base>

<script>
	// ---- Scroll reveal ----
	const revealObserver = new IntersectionObserver((entries) => {
		entries.forEach((entry) => {
			if (entry.isIntersecting) {
				entry.target.classList.add('revealed');
				revealObserver.unobserve(entry.target);
			}
		});
	}, { threshold: 0.15 });

	document.querySelectorAll('.story, .sorting, .clarity, .tools, .paths, .explore, .printables').forEach((el) => {
		revealObserver.observe(el);
	});
</script>

<style>
	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 32px;
	}

	.eyebrow {
		font-size: 0.75rem;
		font-weight: 600;
		letter-spacing: 0.15em;
		text-transform: uppercase;
		color: var(--primary);
		margin: 0 0 1rem;
	}

	.section-lede {
		font-size: 1.15rem;
		color: var(--ink-soft);
		line-height: 1.6;
	}

	/* Buttons */
	.btn {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		padding: 0.9rem 1.85rem;
		border-radius: 10px;
		font-weight: 700;
		font-size: 1rem;
		transition: all var(--duration-fast) ease;
		cursor: pointer;
		text-decoration: none;
	}
	.btn-primary {
		background: var(--primary);
		color: #fff;
		border: none;
		box-shadow: 0 10px 24px rgba(74, 107, 90, 0.35);
	}
	.btn-primary:hover {
		background: var(--primary-dark);
		transform: translateY(-2px);
		box-shadow: 0 14px 30px rgba(74, 107, 90, 0.42);
	}
	.btn-secondary {
		background: #fff;
		color: var(--primary-dark);
		border: 1px solid var(--border);
		box-shadow: 0 8px 20px rgba(28, 29, 31, 0.10);
	}
	.btn-secondary:hover {
		transform: translateY(-2px);
		box-shadow: 0 12px 26px rgba(28, 29, 31, 0.14);
	}
	.btn-on-color {
		background: #fff;
		color: var(--primary-dark);
		border: none;
		box-shadow: 0 8px 20px rgba(0, 0, 0, 0.18);
	}
	.btn-on-color:hover {
		transform: translateY(-2px);
		box-shadow: 0 12px 26px rgba(0, 0, 0, 0.24);
	}

	/* ===== HERO ===== */
	.hero {
		background: linear-gradient(180deg, #F7F5F0 0%, #FAFAF8 100%);
		padding: 3rem 0 4rem;
	}
	.hero-grid {
		display: grid;
		grid-template-columns: minmax(0, 0.95fr) minmax(0, 1.05fr);
		gap: 3rem;
		align-items: center;
	}
	.hero-copy {
		position: relative;
		z-index: 1;
	}
	.hero-copy h1 {
		font-size: clamp(2.25rem, 4.5vw, 3.5rem);
		line-height: 1.08;
		letter-spacing: -0.03em;
		margin-bottom: 1.25rem;
	}
	.text-accent {
		color: var(--primary);
	}
	.hero-lede {
		font-size: 1.15rem;
		line-height: 1.6;
		color: var(--ink-soft);
		max-width: 520px;
		margin-bottom: 1.75rem;
	}
	.hero-actions {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
		margin-bottom: 1.25rem;
	}
	.hero-trust {
		display: flex;
		gap: 0.5rem;
		font-size: 0.85rem;
		color: var(--ink-muted);
	}
	.hero-visual {
		position: relative;
	}
	.hero-image-wrap {
		position: relative;
		border-radius: 24px;
		background: #EFECE6;
		border: 1px solid var(--border);
		box-shadow: 0 24px 60px rgba(28, 29, 31, 0.10);
	}
	.hero-image-wrap img {
		width: 100%;
		height: auto;
		display: block;
		object-fit: contain;
	}
	.hero-float-card {
		position: absolute;
		left: -24px;
		bottom: 24px;
		background: #fff;
		border: 1px solid var(--border);
		border-radius: 16px;
		padding: 1.1rem 1.25rem;
		box-shadow: 0 16px 40px rgba(28, 29, 31, 0.12);
		max-width: 260px;
	}
	.hero-float-title {
		font-family: var(--display);
		font-weight: 700;
		font-size: 1rem;
		margin: 0 0 0.6rem;
		color: var(--ink);
	}
	.hero-float-list {
		list-style: none;
		margin: 0;
		padding: 0;
		display: grid;
		gap: 0.35rem;
	}
	.hero-float-list li {
		font-size: 0.9rem;
		color: var(--ink-soft);
		padding-left: 1.4rem;
		position: relative;
	}
	.hero-float-list li::before {
		content: '✓';
		position: absolute;
		left: 0;
		color: var(--primary);
		font-weight: 700;
	}

	/* ===== FEATURE STRIP ===== */
	.feature-strip {
		border-top: 1px solid var(--border);
		border-bottom: 1px solid var(--border);
		background: #fff;
		padding: 1.75rem 0;
	}
	.feature-grid {
		display: grid;
		grid-template-columns: repeat(4, 1fr);
		gap: 1.25rem;
		align-items: center;
	}
	.feature {
		display: flex;
		align-items: center;
		gap: 0.9rem;
	}
	.feature-icon {
		width: 40px;
		height: 40px;
		border-radius: 10px;
		background: var(--primary-light);
		color: var(--primary-dark);
		display: flex;
		align-items: center;
		justify-content: center;
		flex-shrink: 0;
	}
	.feature-icon svg {
		width: 20px;
		height: 20px;
	}
	.feature-title {
		font-family: var(--display);
		font-weight: 700;
		font-size: 0.95rem;
		margin: 0 0 0.15rem;
		color: var(--ink);
	}
	.feature-desc {
		font-size: 0.85rem;
		color: var(--ink-soft);
		margin: 0;
		line-height: 1.4;
	}

	/* ===== SCROLL STORY ===== */
	.story, .sorting, .clarity, .tools, .paths, .explore, .printables {
		opacity: 0;
		transform: translateY(24px);
		transition: opacity 0.6s var(--ease-out), transform 0.6s var(--ease-out);
	}
	.story.revealed, .sorting.revealed, .clarity.revealed, .tools.revealed, .paths.revealed, .explore.revealed, .printables.revealed {
		opacity: 1;
		transform: translateY(0);
	}

	/* CHAOS */
	.story {
		padding: 4rem 0;
		background: var(--bg-warm);
		overflow-x: hidden;
	}
	.story-inner {
		display: grid;
		grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
		gap: 3rem;
		align-items: center;
	}
	.story-copy {
		max-width: 640px;
	}
	.story-copy h2 {
		font-size: clamp(2rem, 4vw, 3rem);
		margin-bottom: 1rem;
	}
	.story-visual img {
		width: 100%;
		height: auto;
		display: block;
		object-fit: contain;
		border-radius: 20px;
		background: #EFECE6;
		border: 1px solid var(--border);
		box-shadow: 0 20px 50px rgba(28, 29, 31, 0.10);
	}

	/* SORTING */
	.sorting {
		padding: 4rem 0;
		position: relative;
		overflow-x: hidden;
	}
	.sorting-inner {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 3rem;
		align-items: center;
	}
	.sorting-copy h2 {
		font-size: clamp(2rem, 4vw, 3rem);
		margin-bottom: 1rem;
	}
	.sorting-flow {
		display: flex;
		align-items: center;
		gap: 1.5rem;
	}
	.flow-in {
		flex: 1;
		background: var(--bg-warm);
		border: 1px solid var(--border);
		border-radius: 16px;
		padding: 2rem;
		text-align: center;
		font-weight: 600;
		color: var(--ink-soft);
	}
	.flow-arrow {
		font-size: 1.5rem;
		color: var(--primary);
	}
	.flow-out {
		flex: 1;
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}
	.flow-out div {
		background: var(--card);
		border: 1px solid var(--border);
		border-radius: 12px;
		padding: 1rem;
		text-align: center;
	}
	.flow-out strong {
		color: var(--primary);
		font-size: 0.85rem;
		text-transform: uppercase;
		letter-spacing: 0.05em;
	}

	/* CLARITY */
	.clarity {
		padding: 4rem 0;
		background: var(--primary-light);
		text-align: center;
	}
	.clarity-inner {
		max-width: 640px;
		margin: 0 auto;
	}
	.clarity h2 {
		font-size: clamp(2rem, 4vw, 3rem);
		margin-bottom: 1rem;
	}

	/* ===== TOOLS ===== */
	.tools {
		padding: 4rem 0;
	}
	.section-head {
		max-width: 640px;
		margin-bottom: 2.5rem;
	}
	.section-head h2 {
		font-size: clamp(2rem, 4vw, 3rem);
	}
	.tools-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1.5rem;
	}
	.tool-card {
		text-decoration: none;
		color: var(--ink);
		display: block;
	}
	.tool-surface {
		position: relative;
		padding: 2.25rem;
		background: var(--card);
		border: 1px solid var(--border);
		border-radius: 20px;
		box-shadow: 0 12px 32px rgba(28, 29, 31, 0.06);
		transition: transform var(--duration-normal) var(--ease-out), box-shadow var(--duration-normal) var(--ease-out);
	}
	.tool-card:hover .tool-surface {
		transform: translateY(-4px);
		box-shadow: 0 20px 48px rgba(28, 29, 31, 0.10);
	}
	.tool-tag {
		display: inline-block;
		padding: 0.35rem 0.8rem;
		border-radius: 999px;
		color: #fff;
		font-size: 0.7rem;
		font-weight: 700;
		letter-spacing: 0.05em;
		margin-bottom: 1.25rem;
	}
	.tool-card.c-purple .tool-surface { background: #EEEDFE; }
	.tool-card.c-purple .tool-tag { background: #7F77DD; }
	.tool-card.c-purple h3 { color: #26215C; }
	.tool-card.c-purple .tool-link { color: #534AB7; }
	.tool-card.c-green .tool-surface { background: #EAF3DE; }
	.tool-card.c-green .tool-tag { background: #639922; }
	.tool-card.c-green h3 { color: #173404; }
	.tool-card.c-green .tool-link { color: #3B6D11; }
	.tool-card.c-amber .tool-surface { background: #FAEEDA; }
	.tool-card.c-amber .tool-tag { background: #BA7517; }
	.tool-card.c-amber h3 { color: #412402; }
	.tool-card.c-amber .tool-link { color: #854F0B; }
	.tool-surface h3 {
		font-size: 1.5rem;
		margin-bottom: 0.5rem;
	}
	.tool-surface p {
		color: var(--ink-soft);
		font-size: 1rem;
		margin-bottom: 1.5rem;
	}
	.tool-link {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		font-weight: 700;
		font-size: 0.95rem;
	}
	.arrow {
		transition: transform var(--duration-fast) ease;
	}
	.tool-card:hover .arrow,
	.path-card:hover .arrow {
		transform: translateX(4px);
	}

	/* ===== PATHS ===== */
	.paths {
		padding: 4rem 0;
		background: var(--bg-warm);
	}
	.paths-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1.5rem;
	}
	.path-card {
		padding: 2.25rem;
		border-radius: 20px;
		text-decoration: none;
		display: block;
		transition: transform var(--duration-normal) var(--ease-out), box-shadow var(--duration-normal) var(--ease-out);
		color: #fff;
	}
	.path-card:hover {
		transform: translateY(-4px);
		box-shadow: 0 20px 48px rgba(28, 29, 31, 0.15);
	}
	.path-card.c-green { background: #4A6B5A; }
	.path-card.c-green .path-num { color: #C0DD97; }
	.path-card.c-green .path-link { color: #E8F1EA; }
	.path-card.c-amber { background: #D98E3F; color: #412402; }
	.path-card.c-amber .path-num { color: #fff; }
	.path-card.c-amber p { color: #633806; }
	.path-card.c-amber .path-link { color: #412402; }
	.path-card.c-purple { background: #26215C; }
	.path-card.c-purple .path-num { color: #AFA9EC; }
	.path-card.c-purple .path-link { color: #CECBF6; }
	.path-num {
		display: block;
		font-family: var(--display);
		font-size: 2.5rem;
		font-weight: 700;
		margin-bottom: 1.5rem;
	}
	.path-card h3 {
		font-size: 1.75rem;
		margin-bottom: 0.75rem;
	}
	.path-card p {
		color: rgba(255,255,255,0.85);
		font-size: 1rem;
		line-height: 1.6;
		margin-bottom: 1.5rem;
	}
	.path-link {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		font-weight: 700;
		font-size: 0.95rem;
	}

	/* ===== EXPLORE ===== */
	.explore {
		padding: 4rem 0;
		background: var(--bg-warm);
	}
	.explore-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1.5rem;
	}
	.explore-card {
		display: block;
		padding: 2.25rem;
		background: var(--card);
		border: 1px solid var(--border);
		border-radius: 20px;
		text-decoration: none;
		color: var(--ink);
		transition: transform var(--duration-normal) var(--ease-out), box-shadow var(--duration-normal) var(--ease-out);
	}
	.explore-card:hover {
		transform: translateY(-4px);
		box-shadow: 0 20px 48px rgba(28, 29, 31, 0.10);
	}
	.explore-card h3 {
		font-size: 1.5rem;
		margin-bottom: 0.6rem;
	}
	.explore-card p {
		color: var(--ink-soft);
		font-size: 1rem;
		margin-bottom: 1.25rem;
	}
	.explore-link {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		font-weight: 700;
		font-size: 0.95rem;
		color: var(--primary);
	}

	/* ===== PRINTABLES ===== */
	.printables {
		padding: 4rem 0;
	}
	.printables-inner {
		position: relative;
		overflow: hidden;
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 3rem;
		align-items: center;
		background: var(--primary);
		border-radius: 24px;
		padding: 3.5rem;
		box-shadow: 0 24px 60px rgba(74, 107, 90, 0.25);
	}
	.printables-copy h2 {
		font-size: clamp(2rem, 4vw, 2.75rem);
		margin-bottom: 1rem;
		color: #fff;
	}
	.printables-copy .section-lede {
		margin-bottom: 2rem;
		color: var(--primary-light);
	}
	.printables-visual {
		display: flex;
		align-items: center;
		justify-content: center;
		perspective: 1000px;
	}
	.planner {
		position: relative;
		width: 240px;
		height: 320px;
		transform-style: preserve-3d;
		transform: rotateY(-12deg) rotateX(4deg);
	}
	.planner-page {
		position: absolute;
		inset: 0;
		background: var(--card);
		border: 1px solid var(--border);
		border-radius: 6px;
		box-shadow: 0 12px 32px rgba(28, 29, 31, 0.12);
	}
	.page-back { transform: translateZ(-12px) translateX(6px); }
	.page-mid { transform: translateZ(-6px) translateX(3px); }
	.page-front {
		transform: translateZ(0);
		padding: 1.5rem;
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
	}
	.planner-line {
		display: block;
		height: 8px;
		background: var(--bg-warm);
		border-radius: 4px;
	}
	.planner-line.short { width: 60%; }
	.planner-spine {
		position: absolute;
		left: -6px;
		top: 0;
		bottom: 0;
		width: 12px;
		background: var(--primary);
		border-radius: 3px 0 0 3px;
	}

	/* ===== RESPONSIVE ===== */
	@media (max-width: 1024px) {
		.hero-grid {
			grid-template-columns: 1fr;
			gap: 2.5rem;
		}
		.hero-image-wrap {
			max-width: 560px;
		}
		.hero-float-card {
			left: 12px;
			bottom: 12px;
		}
		.tools-grid, .paths-grid, .explore-grid {
			grid-template-columns: 1fr 1fr;
		}
		.story-inner, .sorting-inner, .printables-inner {
			grid-template-columns: 1fr;
			gap: 2.5rem;
		}
	}

	@media (max-width: 768px) {
		.tools-grid, .paths-grid, .explore-grid {
			grid-template-columns: 1fr;
		}
		.container {
			padding: 0 20px;
		}
		.hero {
			padding: 2.5rem 0 3rem;
		}
		.hero-copy h1 {
			font-size: clamp(1.9rem, 6vw, 2.5rem);
		}
		.hero-lede {
			font-size: 1.05rem;
		}
		.hero-actions {
			flex-direction: column;
		}
		.btn {
			width: 100%;
		}
		.hero-image-wrap {
			max-width: 100%;
		}
		.hero-float-card {
			position: static;
			max-width: 100%;
			margin-top: 1rem;
		}
		.feature-grid {
			grid-template-columns: 1fr 1fr;
			gap: 1rem;
		}
		.sorting-flow {
			flex-direction: column;
		}
		.flow-arrow {
			transform: rotate(90deg);
		}
	}

	@media (max-width: 480px) {
		.hero-copy h1 {
			font-size: 1.85rem;
		}
		.hero-lede {
			font-size: 1rem;
		}
		.feature-grid {
			grid-template-columns: 1fr;
		}
		.printables-inner {
			padding: 2rem;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.story, .sorting, .clarity, .tools, .paths, .printables {
			opacity: 1;
			transform: none;
			transition: none;
		}
		.tool-card:hover .tool-surface,
		.path-card:hover {
			transform: none;
		}
	}
</style>